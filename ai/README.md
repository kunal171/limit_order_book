# AI Analysis Module

Status: paused.

The AI foundation exists and is documented, but active work is paused while the
project shifts toward HFT-style systems improvements.

Resume notes:

```text
docs/AI_WORK_PAUSE.md
```

This folder contains the first AI-facing layer for the limit order book project.

The Rust engine produces deterministic run artifacts. This module reads those
artifacts and creates a human-readable analysis report.

## Current Scope

The current analyzer is intentionally simple and deterministic:

```text
run artifacts -> Python analyzer -> analysis.md
```

It does not call an LLM yet. That is deliberate. Before adding LangChain or
LangGraph, we want a clean input/output contract that is easy to test and easy
to plug into Windmill.

## Inputs

Each run directory should contain:

```text
summary.json
events.json
snapshot.json
```

These files are generated by the Rust CLI when using `--output-dir`.

Example:

```bash
RUN_PREFIX=ai-test ./scripts/run_simulation.sh synthetic-crossing 100 auto
```

## Output

The analyzer writes:

```text
analysis.md
```

The report includes:

```text
scenario summary
book metrics
trade metrics
event counts
basic interpretation
```

## Usage

Run the analyzer against one completed run folder:

```bash
python3 ai/analyze_run.py runs/ai-test-20260902-120000
```

After it runs, open:

```bash
cat runs/ai-test-20260902-120000/analysis.md
```

## Why This Exists

The matching engine should stay Rust-only, deterministic, and fast.

The AI layer should sit outside the hot path and analyze finished artifacts.
This keeps the trading logic clean while still allowing reports, debugging
helpers, and later LangChain/LangGraph workflows.

## Next Steps

Deferred additions:

```text
richer deterministic interpretation
LLM-generated report summaries
LangChain tool wrapper around the analyzer
LangGraph workflow for repeated experiments
Windmill job that runs simulation and then writes analysis.md
```
